package org.dam.tfg.repositories
/* import android.content.Context
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.Json
import org.dam.tfg.models.table.Mesa

class BudgetRepositoryAndroid(private val context: Context) : BudgetRepository {
    private val sharedPreferences = context.getSharedPreferences("budget_data", Context.MODE_PRIVATE)

    override fun saveMesa(mesa: Mesa) {
        sharedPreferences.edit().putString("mesa_data", Json.encodeToString(mesa)).apply()
    }
} */